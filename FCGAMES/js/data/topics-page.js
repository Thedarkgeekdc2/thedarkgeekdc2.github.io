const qs=new URLSearchParams(location.search); const classNo=Number(qs.get('class')); const subject=qs.get('subject');
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function load(p){const r=await fetch('../'+p,{cache:'no-store'});if(!r.ok)throw Error('Could not load '+p);return r.json();}
async function boot(){try{const [school,app,index]=await Promise.all([load('config/school-config.json'),load('config/app-config.json'),load('data/topics.json')]);const topics=index.topics.filter(t=>t.class===classNo&&t.subject===subject);document.getElementById('topics-app').innerHTML=`<div class="app"><header class="navbar"><div class="brand"><img src="../${esc(school.logo)}" alt=""><div class="brand-text"><div class="app-name">${esc(app.appName)}</div><div class="school-name">${esc(school.schoolName)}</div></div></div><a class="secondary-btn" href="../index.html">🏠 Home</a></header><main class="main"><div class="hero-card"><div class="hero-kicker">Choose a topic</div><h1>${classNo===3?'🧮 Maths':'📖 Hindi'}</h1><p>Select a topic to start your flashcard challenge.</p></div><section class="section"><div class="class-grid topic-grid">${topics.map(t=>`<article class="class-card"><div class="class-top"><div><div class="hero-kicker">Topic</div><h3>${esc(t.icon||'🃏')} ${esc(t.name)}</h3></div><div class="class-icon">${esc(t.icon||'🃏')}</div></div><p>${esc(t.description||'Practice with flashcards.')}</p><button class="primary-btn" data-topic="${esc(t.id)}">Start Topic</button></article>`).join('')}</div></section></main>${school.showFooter?`<footer class="footer"><div class="footer-inner"><div class="footer-main">${esc(school.copyrightText)}</div><div class="footer-credit">${esc(school.footerText)}</div></div></footer>`:''}</div>`;document.querySelectorAll('[data-topic]').forEach(b=>b.addEventListener('click',()=>location.href=`difficulty.html?class=${encodeURIComponent(classNo)}&subject=${encodeURIComponent(subject)}&topic=${encodeURIComponent(b.dataset.topic)}`));}catch(e){document.body.innerHTML=`<main class="main"><div class="hero-card"><h1>Topic Error</h1><p>${esc(e.message)}</p><a class="primary-btn" href="../index.html">Back Home</a></div></main>`;}}boot();